---
description: Always apply this rule to avoid wasting tokens on unnecessary cd commands.
alwaysApply: false
---

The working directory is already the project root. Never prefix commands with `cd /path/to/project`. Just run commands directly from the current directory.